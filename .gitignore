node_modules/
out/
dist/
release/
*.log
*.local
.DS_Store
.env

# OAuth client cho DEV — file JSON tải từ Google Console, thả vào gốc repo để `pnpm dev`
# tự nhận (xem apps/desktop/src/main/lib/googleDrive.ts). Bản release dùng hằng nhúng cứng.
.google-oauth.json

# Quy tắc làm việc cho Claude Code — giữ ở máy, không publish
CLAUDE.md

# Cấu hình editor cá nhân (vd tắt warning vars của extension GitHub Actions) — không ép người clone
.vscode/

# Harness chụp ảnh UI dựng tạm mỗi phiên (bundle + CSS build + ảnh PNG + script probe).
# Máy này không có rasterizer SVG nên cách duy nhất TỰ NHÌN thứ mình vừa vẽ là bundle bằng
# esbuild rồi chụp bằng Electron `capturePage` — xem CLAUDE.md §7. Sản phẩm của việc đó là
# file tạm, không phải mã nguồn.
apps/desktop/shot/
